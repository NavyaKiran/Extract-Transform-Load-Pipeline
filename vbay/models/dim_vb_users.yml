version: 2
models: 
  - name: dim_vb_users
    description: One row per user 
    columns: 
      - name: userkey 
        description: The surrogate key for user. 
        tests: 
          - not_null
          - unique 
      - name: user_id 
        description: THe business/ source key for user 
        tests: 
          - not_null
          - unique 